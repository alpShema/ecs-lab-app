version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <TASK_DEFINITION>   # placeholder — CodeDeploy fills this in
        LoadBalancerInfo:
          ContainerName: ecs-lab-container  # must match ContainerDefinitions name in ecs.yaml
          ContainerPort: 8080
        PlatformVersion: LATEST
